const person = prompt('who is there?')

if (person === 'cancel') {
    alert('Canceled');

} else(person === 'admin');

const password = prompt('password')
if (password === 'cancel') {
    alert('Canceled')

} else(password === '123');{
    alert('Welcome')
}